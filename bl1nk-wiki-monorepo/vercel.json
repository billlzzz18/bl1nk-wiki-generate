{
  "version": 2,
  "builds": [
    {
      "src": "apps/frontend/package.json",
      "use": "@vercel/next",
      "config": { "distDir": "apps/frontend/.next" }
    }
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD . || git diff --quiet HEAD^ HEAD apps/frontend"
}